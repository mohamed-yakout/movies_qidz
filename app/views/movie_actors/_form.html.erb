<%= form_with(model: movie_actor, local: true) do |form| %>
  <% if movie_actor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie_actor.errors.count, "error") %> prohibited this movie_actor from being saved:</h2>

      <ul>
      <% movie_actor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :movie_id %>
    <%= form.text_field :movie_id %>
  </div>

  <div class="field">
    <%= form.label :actor_id %>
    <%= form.text_field :actor_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
